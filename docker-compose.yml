# PWallet — فقط اپلیکیشن

services:
  pwallet:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pwallet
    restart: unless-stopped
    ports:
      - "127.0.0.1:3080:80"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
